.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix
.quartz/
quartz/cli/tui/dist/
.turbo/

# Vault tooling that `quartz sync` copies in with the notes: agent settings and
# hooks, MCP config, caches. Not site content, and this repository is public.
content/.claude/
content/.claudian/
content/.mcp.json
content/.ruff_cache/
content/CLAUDE.md
