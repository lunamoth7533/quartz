# Obsidian Vault - research library

The vault's rules live in `Research/AGENTS.md`, imported here so Claude Code loads them:

@Research/AGENTS.md

This is not an Obsidian Mind vault. The global instructions about `work/`, `brain/`, `perf/`, `org/` and the `om-*` commands and hooks do not apply here; follow `Research/AGENTS.md`.
